/**
 * navigation-router-cross-module — 跨模块路由（NavPushPathHelper）
 * 本项目为单模块结构，无法实际演示跨模块跳转，保留 UI 骨架 + 黄色提示卡片说明限制
 * 元服务兼容性：NavPushPathHelper 全部方法从 API 12+ 支持
 */
import { BusinessError } from '@kit.BasicServicesKit'

@ComponentV2
export struct NavigationRouterCrossModule {
  @Local resultText: string = '跨模块路由需要多 HSP 分包项目结构，当前为单模块项目'

  build() {
    Scroll() {
      Column({ space: 16 }) {
        Text('跨模块路由 NavPushPathHelper')
          .fontSize(22)
          .fontWeight(FontWeight.Bold)
          .margin({ top: 20, bottom: 10 })

        // 结果展示区
        Column() {
          Text(this.resultText)
            .fontSize(14)
            .fontColor('#333333')
            .width('100%')
            .padding(12)
        }
        .width('90%')
        .backgroundColor('#F5F5F5')
        .borderRadius(8)

        // 不支持说明卡片
        Column() {
          Row() {
            Text('!')
              .fontSize(16)
              .fontWeight(FontWeight.Bold)
              .fontColor('#FF6B00')
              .margin({ right: 8 })
            Text('跨模块路由限制')
              .fontSize(14)
              .fontWeight(FontWeight.Bold)
              .fontColor('#FF6B00')
          }
          .width('100%')
          .margin({ bottom: 8 })
          Text('当前项目为单模块（entry）结构，无法实际演示 NavPushPathHelper 跨模块跳转。')
            .fontSize(12)
            .fontColor('#666666')
            .width('100%')
          Text('跨模块路由需要：主包 + 至少一个 HSP 分包，且分包未在主包中直接依赖。')
            .fontSize(12)
            .fontColor('#666666')
            .width('100%')
            .margin({ top: 4 })
          Text('NavPushPathHelper 会在跳转时检查分包是否已安装，未安装则自动下载后跳转。')
            .fontSize(12)
            .fontColor('#333333')
            .width('100%')
            .margin({ top: 4 })
        }
        .width('90%')
        .backgroundColor('#FFF9E6')
        .borderRadius(8)
        .padding(12)
        .border({ width: 1, color: '#FFE0B2' })

        // NavPushPathHelper API 参考
        Column() {
          Text('NavPushPathHelper API 参考')
            .fontSize(14)
            .fontWeight(FontWeight.Bold)
            .width('100%')
            .margin({ bottom: 8 })

          Text('构造函数')
            .fontSize(13)
            .fontWeight(FontWeight.Medium)
            .fontColor('#333333')
            .width('100%')
            .margin({ top: 4 })
          Text('constructor(navPathStack: NavPathStack)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')

          Text('跳转方法（均返回 Promise<void>）')
            .fontSize(13)
            .fontWeight(FontWeight.Medium)
            .fontColor('#333333')
            .width('100%')
            .margin({ top: 8 })
          Text('pushPath(moduleName, info, animated?)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('pushPathByName(moduleName, name, param, animated?)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('pushDestination(moduleName, info, animated?)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('pushDestinationByName(moduleName, name, param, animated?)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('replacePath(moduleName, info, animated?)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('replacePathByName(moduleName, name, param, animated?)')
            .fontSize(12)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')

          Text('moduleName: HSP 分包名称，每个方法的首个参数')
            .fontSize(12)
            .fontColor('#999999')
            .width('100%')
            .margin({ top: 4 })

          Text('使用示例:')
            .fontSize(13)
            .fontWeight(FontWeight.Medium)
            .fontColor('#333333')
            .width('100%')
            .margin({ top: 8 })
          Text('import { NavPushPathHelper } from \'@kit.ArkUI\'')
            .fontSize(11)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('helper = new NavPushPathHelper(this.pathStack)')
            .fontSize(11)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('helper.pushPath(\'product\', { name: \'List\' }, false)')
            .fontSize(11)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('  .then(() => { /* 成功 */ })')
            .fontSize(11)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')
          Text('  .catch((err: BusinessError) => { /* 失败 */ })')
            .fontSize(11)
            .fontColor('#666666')
            .fontFamily('monospace')
            .width('100%')

          Text('注意事项:')
            .fontSize(13)
            .fontWeight(FontWeight.Medium)
            .fontColor('#333333')
            .width('100%')
            .margin({ top: 8 })
          Text('1. HSP 分包 module.json5 中必须删除 pages 配置，否则路由白屏')
            .fontSize(12)
            .fontColor('#CC0000')
            .width('100%')
          Text('2. 调试时需在 Run > Edit Configurations 中勾选 Deploy Multi Hap')
            .fontSize(12)
            .fontColor('#666666')
            .width('100%')
          Text('3. 仅支持 HSP 分包，不支持 APP_PACKAGE 类型独立分包')
            .fontSize(12)
            .fontColor('#666666')
            .width('100%')

          Text('元服务：NavPushPathHelper 全部方法从 API 12+ 支持')
            .fontSize(12)
            .fontColor('#999999')
            .width('100%')
            .margin({ top: 4 })
        }
        .width('90%')
        .backgroundColor('#FFF9E6')
        .borderRadius(8)
        .padding(12)
        .margin({ top: 8 })
      }
      .width('100%')
      .alignItems(HorizontalAlign.Center)
    }
    .width('100%')
    .height('100%')
  }
}
