# -*- coding: utf-8 -*-
"""跨模块共享的默认常量。"""

"""共享默认常量。

这些常量只作为代码侧兜底值使用：
- 配置文件里显式配置时，优先使用配置值
- 配置缺失或读取不到时，才回落到这里的默认值
"""

# Agent 执行超时默认值（秒）。
# 规则：agents.yaml 中配置了 timeout 时优先使用配置；未配置时回落到 600 秒。
DEFAULT_TIMEOUT_SECONDS = 600
